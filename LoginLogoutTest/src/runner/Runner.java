package runner;

import org.junit.runner.RunWith;
import cucumber.api.junit.*;

@RunWith(Cucumber.class)
@Cucumber.Options(features="features", glue={"stepDefiniton"})

public class Runner {
// Sorry, but the script I have created is automation script for website testing using Cucumber
//	because I have problem while installing Katalon Studio and Robot Framework on my device, 
// the problem is related to my installed OS (I use Manjaro Linux) 
}
